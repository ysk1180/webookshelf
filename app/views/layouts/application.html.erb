<!DOCTYPE html>
<html>
  <head>
    <title>Web本棚</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%# レスポンシブ対応 %>
    <meta name="viewport" content="width=device-width">

    <%# モーダル %>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2"></script>

    <%# ファビコン設定 %>
    <%= favicon_link_tag %>

    <%# Twitterカード %>
    <% twitter_card = get_twitter_card_info(@h? @h: nil) %>
    <meta name="twitter:card" content=<%=twitter_card[:card]%> />
    <meta property="og:url" content=<%=twitter_card[:url]%> />
    <meta property="og:title" content=<%=twitter_card[:title]%> />
    <meta property="og:description" content=<%=twitter_card[:description]%> />
    <meta property="og:image" content=<%=twitter_card[:image]%> />

    <%# Global site tag (gtag.js) - Google Analytics %>
    <%= analytics_init if Rails.env.production? %>
  </head>

  <body>
    <div class="header">
      <a href="/" style="color: white;"><i class="fa fa-book fa-fw"></i>Web本棚</a>
      <span id='sub-title'>あなただけの本棚をシェアしよう</span>
    </div>
    <div class="container">
      <%= yield %>
    </div>
    <footer>
      <div>
        <%= link_to '利用規約', terms_path %> / <%= link_to 'プライバシーポリシー', privacy_path %>
      </div>
      <div>
        <a href="https://twitter.com/ysk_pro" target="_blank"><i class="fa fa-twitter fa-fw"></i>開発者Twitterアカウント</a>
      </div>
    </footer>
  </body>
</html>
